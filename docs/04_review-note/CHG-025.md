## CHG-025 review-note

## Final Gate 結果

- `cd backend && ./mvnw test` → BUILD SUCCESS（Tests run: 142, Failures: 0, Errors: 0）
- `cd backend && ./mvnw clean test` → BUILD SUCCESS（Tests run: 142, Failures: 0, Errors: 0）
- `target/classes/db/` 配下に `archive/` ディレクトリが存在しないことを確認

## Review Packet

### 変更サマリ（10行以内）

- `backend/src/main/resources/db/archive/` を `backend/db-archive/` へ `git mv` で移動（Maven リソーススキャン対象外）
- `backend/db-archive/flyway-v2-to-v12/README.md` のパス参照を更新
- `docs/agent-rules/testing-operations.md` に `./mvnw test`（clean なし）が使用可能な旨を明記
- `./mvnw clean` を実行し、`target/` の stale ファイルを除去

### リスクと未解決

- なし。新規クローン環境では `target/` が存在しないため stale ファイルは発生しない。本 CHG 実装時に `mvn clean` を実行済み。

### テスト結果（PASS/FAIL）

- PASS: `cd backend && ./mvnw test`（142テスト、Flyway V2 重複エラーなし）
- PASS: `cd backend && ./mvnw clean test`（142テスト）
